#include "pch.h"

TEST(DataBaseTest, CreateRecordTest) {
	EXPECT_TRUE(false);
}

TEST(DataBaseTest, ReadRecordTest) {
	EXPECT_TRUE(false);
}

TEST(DataBaseTest, UpdateRecordTest) {
	EXPECT_TRUE(false);
}

TEST(DataBaseTest, DeleteRecordTest) {
	EXPECT_TRUE(false);
}

TEST(DataBaseTest, FindAllTest) {
	EXPECT_TRUE(false);
}